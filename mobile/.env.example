# Backend URL. Set this to your computer's IP so the app on your phone can reach the API.
# Get your IP: run "ipconfig getifaddr en0" (Mac) or "ipconfig" (Windows) and use that IP.
# Restart Expo (npx expo start) after changing this.
#
EXPO_PUBLIC_API_URL=http://YOUR_IP_HERE:8000
